{
  "name": "@void/starshine-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VOID-owned durable event relay for Starshine application ledgers.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/capability.test.ts src/file_catalog.test.ts src/file_gateway.test.ts src/schema.test.ts src/outbox.test.ts src/scan.test.ts src/http.test.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "amqplib": "^0.10.9",
    "starshine-sdk-js": "file:.."
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.7",
    "@types/node": "^24.0.13",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
